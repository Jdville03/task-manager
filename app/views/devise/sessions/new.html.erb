<div class="row">
  <div class="col-sm-8 col-sm-offset-2">
    <h2 class="marker">Log in</h2>
    <hr>
  </div>
</div>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="row">
    <div class="col-sm-8 col-sm-8 col-sm-offset-2 form-group <%= "has-error" if flash[:alert] %>">
      <div class="input-group">
        <span class="input-group-addon" id="email-addon"><i class="fa fa-envelope-o fa-fw"></i></span>
        <%= f.email_field :email, autofocus: true, placeholder: "Email address", class: "form-control", 'aria-describedby': "email-addon" %>
      </div><br>
      <div class="input-group">
        <span class="input-group-addon" id="password-addon"><i class="fa fa-key fa-fw"></i></span>
        <%= f.password_field :password, autocomplete: "off", placeholder: "Password", class: "form-control", 'aria-describedby': "password-addon" %>
      </div>
    </div>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="row">
      <div class="form-group col-sm-8 col-sm-8 col-sm-offset-2">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    </div>
  <% end -%>

  <div class="row">
    <div class="form-group col-sm-8 col-sm-8 col-sm-offset-2">
      <%= f.submit "Log in", class: "btn btn-success btn-block" %>
      <div class="text-center">-or-</div>
      <a href="<%= user_facebook_omniauth_authorize_path %>" class="btn btn-block btn-social btn-facebook">
        <span class="fa fa-facebook"></span> Sign in with Facebook
      </a>
    </div>
  </div>
<% end %>

<div class="row">
  <div class="col-sm-8 col-sm-offset-2">
    <p class="text-center text-muted">
      Don't have an account? <%= link_to "Sign up", new_registration_path(resource_name) %><br>
      <%= link_to "Forgot your password?", new_password_path(resource_name) %>
    </p>
  </div>
</div>

<%# render "devise/shared/links" %>
